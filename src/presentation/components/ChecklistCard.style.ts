import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  card: {
    padding: 16,
    borderRadius: 8,
    borderWidth: 1,
    marginBottom: 8,
  },
  title: {
    fontWeight: '500',
  },
  subtitle: {
    marginTop: 4,
    opacity: 0.9,
  },
});
